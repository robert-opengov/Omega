/**
 * GAB Core `apps/web` ships many domain-specific page widgets (CIP, grants, etc.)
 * that are not part of the UI Showcase. Gate them here for optional future ports.
 */
export const GAB_DOMAIN_WIDGETS_ENABLED = false;
